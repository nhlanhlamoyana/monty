#include "monty.h"
/**
 * f_pchar - The char at the top of the stack gets printed, followed by a new line.
 * @head: The head of stack.
 * @counter: The line_number
 * Return: No return
 */
void f_pchar(stack_t **head, unsigned int counter)
{
	stack_t *h

		h = *head;
		if (!h)
		{
			fprintf(stderr, "L%d: can't pchar, stack empty\n", counter);

			fclose(bus.file);

			free_stack(*head);







































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































		ioj

}
